# handTrack

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `brohsha/handTrack` (this fork), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage labels, used as-is with no renaming. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
